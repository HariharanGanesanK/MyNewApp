<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 🌐 Internet & Network Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.HelloWorldApp"
        tools:targetApi="31">

        <!-- 🧾 Registration Page -->
        <activity
            android:name=".RegistrationActivity"
            android:exported="false"
            android:label="Registration"
            android:theme="@style/Theme.HelloWorldApp" />

        <!-- 🔐 Login Page -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:label="Login"
            android:theme="@style/Theme.HelloWorldApp" />

        <!-- 🔒 Biometric / PIN Setup Page -->
        <activity
            android:name=".ProtectionSetupActivity"
            android:exported="false"
            android:label="Protection Setup"
            android:theme="@style/Theme.HelloWorldApp" />

        <!-- 🔓 Authentication Unlock Page -->
        <activity
            android:name=".AuthenticationActivity"
            android:exported="false"
            android:label="Authentication"
            android:theme="@style/Theme.HelloWorldApp" />

        <!-- 📊 Dashboard Page -->
        <activity
            android:name=".DashboardActivity"
            android:exported="false"
            android:label="Dashboard"
            android:theme="@style/Theme.HelloWorldApp" />

        <!-- 🎥 Camera Selection Page -->
        <activity
            android:name=".CamSelectionActivity"
            android:exported="false"
            android:label="Camera Selection"
            android:theme="@style/Theme.HelloWorldApp" />

        <!-- 📷 Cam 1 Page -->
        <activity
            android:name=".Cam1Page"
            android:exported="false"
            android:label="Cam 1"
            android:theme="@style/Theme.HelloWorldApp" />

        <!-- 📸 Cam 2 Page -->
        <activity
            android:name=".Cam2Page"
            android:exported="false"
            android:label="Cam 2"
            android:theme="@style/Theme.HelloWorldApp" />

        <!-- 🚀 Main Entry Point -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.HelloWorldApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
